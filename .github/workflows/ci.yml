name: Node.js CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # Step 1: Checkout code
      - name: Checkout repository
        uses: actions/checkout@v4

      # Step 2: Set up Node.js environment
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      # Step 3: Verify files (for debugging)
      - name: Verify files
        run: ls -R

      # Step 4: Clean old dependencies (keep package-lock.json!)
      - name: Clean old dependencies
        run: rm -rf node_modules

      # Step 5: Install dependencies using lock file
      - name: Install dependencies
        run: npm ci

      # Step 6: Run tests
      - name: Run tests
        run: npm test
